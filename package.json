{
  "name": "newgxt",
  "private": true,
  "workspaces": [
    "level-engine"
  ],
  "scripts": {
    "test": "npm test --workspace=@gxt/level-engine",
    "typecheck": "npm run typecheck --workspace=@gxt/level-engine"
  }
}
